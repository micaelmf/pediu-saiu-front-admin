<!doctype html>
<html lang="en">
  <%- include('partials/header') %>

  <body>
    <div class="flex min-h-screen flex-col items-center justify-center py-5 px-md lg:py-10 lg:px-lg">
      <a class="inline-flex items-center">
        <i data-icon="feather__wind" class="mr-2 h-6 w-6 rotate-180 transform text-primary-600"></i>
        <span class="-skew-x-3 transform text-2xl font-semibold italic text-gray-500">Skewind</span>
      </a>
      <div class="p-10 text-center">
        <h1 class="mb-2 text-4xl font-bold">Entrar</h1>
        <p class="text-lg text-gray-400">Faça login para continuar</p>
      </div>
      <div class="card w-full max-w-md">
        <form class="flex flex-col p-lg">
          <label class="field">
            <span class="field__label">Email</span>
            <span class="form-control">
              <input type="email" name="email" />
            </span>
          </label>
          <label class="field">
            <span class="field__label">Senha</span>
            <span class="form-control">
              <input type="password" name="password" />
            </span>
          </label>
          <div class="flex items-center justify-between">
            <label class="check-control">
              <input type="checkbox" name="remember" />
              <span>Lembrar</span>
            </label>
            <a href="" class="text-right">Esqueceu a senha?</a>
          </div>
          <button type="submit" id="login" class="button button--lg my-10 scheme-primary">Entrar</button>
          <p class="text-center">
            Não tem uma conta?
            <a href="/register.html" class="whitespace-nowrap p-2 font-bold text-scheme-500">Criar conta</a>
          </p>
        </form>
      </div>
      <p class="mt-5 text-sm text-gray-400">&copy; 2023. Todos os direitos reservados</p>
    </div>
  </body>

  <script type="module" src="/js/login.js"></script>
</html>
